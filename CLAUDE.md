# Claude Code Instructions

Before making changes, read and follow `AGENTS.md` in the repository root.

`AGENTS.md` is the canonical repository-wide source for:

- architecture and dependency boundaries
- package and directory responsibilities
- documentation maintenance
- coding and comment conventions
- testing and validation requirements
- Git and contribution rules
- feature, bug-fix, and refactoring workflows

Do not duplicate or override those rules here.

When repository structure, public behavior, architecture, or documentation is
affected, follow the corresponding maintenance requirements in `AGENTS.md`.

If this file conflicts with `AGENTS.md`, follow `AGENTS.md` unless the repository
maintainer explicitly instructs otherwise.